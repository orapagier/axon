<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service config for auto-answer.
    We listen for WINDOW_STATE_CHANGED events so we can detect when the
    incoming call screen appears and programmatically "press" the answer button.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description"
    android:packageNames="com.android.incallui,com.google.android.dialer,com.samsung.android.incallui,com.android.dialer" />
